#include "query.h"
#include "schema.h"

int main(){
    std::vector<Schema> tempSchemas;
    Schema tempSchema;
    tempSchemas.push_back(tempSchema);
    do{
        

    } while(next != 'SELECT');

    return 0;
}