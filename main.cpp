#include "query.h"
#include "schema.h"

int main(){
    std::vector<Schema> tempSchemas;
    std::string next;
    Schema tempSchema;
    tempSchemas.push_back(tempSchema);

    Query testquery;
    testquery.getQuery();
    return 0;
}